

resources:
  - name: sample_resource
    type: image
    integration: ship_int
    versionTemplate:
      region: us-east-2
      sourceName: aws-cluster
      versionName: latest

  - name: sample_resource_2
    type: image
    integration: ship_int
    versionTemplate:
      region: us-east-2
      sourceName: aws-cluster
      versionName: latest

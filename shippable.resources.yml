vars:
  region: us-west-2   
  sourceName: aws_default_cluster
  integration: ship_int

resources:
  - name: sample_resource
    type: image
    integration: ${integration}
    versionTemplate:
      region: ${region}
      sourceName: ${sourceName}
      versionName: latest

  - name: sample_resource_2
    type: image
    integration: ${integration}
    versionTemplate:
      region: ${region}
      sourceName: ${sourceName}
      versionName: latest
